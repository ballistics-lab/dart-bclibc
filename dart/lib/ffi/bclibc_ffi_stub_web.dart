// Web placeholder for bclibc_ffi.dart's conditional export in bclibc.dart.
//
// bclibc_ffi.dart is the dart:ffi-based native binding and has no web
// equivalent in this package — the real web (wasm/dart:js_interop) binding,
// bclibc_ffi_web.dart, lives in dart_bclibc_flutter instead, since it's only
// realistically usable from a Flutter Web app (its wasm asset is served via
// Flutter's asset bundling).
